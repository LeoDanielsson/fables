## Usage

#### Install dependencies
* `npm install`

#### Run locally
* `npm run start`
* Open `http://localhost:3003` in browser

#### Build static assets
* `npm run build`
* Assets can be found under `/public`
